{
  "name": "richfolio-scheduler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that fires richfolio's GitHub Actions workflows on time, replacing GitHub's own scheduler.",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
